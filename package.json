{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "plugins/*",
    "examples/*",
    "create-revite-app",
    "servable"
  ],
  "scripts": {
    "dev": "lerna run tsc --stream --parallel",
    "demo": "cd examples/basic && yarn dev",
    "demo-ssr": "cd examples/basic-ssr && yarn dev",
    "demo:build": "cd examples/basic && yarn build",
    "demo:start": "cd examples/basic && yarn start",
    "demo:serve": "cd examples/basic && yarn serve"
  },
  "devDependencies": {
    "@types/node": "^14.14.25",
    "@types/react": "^17.0.1",
    "@types/react-dom": "^17.0.0",
    "lerna": "^3.22.1"
  },
  "dependencies": {}
}
